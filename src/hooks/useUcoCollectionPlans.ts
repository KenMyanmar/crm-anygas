
// Re-export hooks for backward compatibility
export { useUcoPlans as useUcoCollectionPlans } from './useUcoPlans';
export { useUcoItems as useUcoCollectionItems } from './useUcoItems';

// Re-export types for backward compatibility
export type { UcoCollectionPlan, UcoCollectionItem } from '@/types/ucoCollection';
